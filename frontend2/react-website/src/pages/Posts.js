import React, { useRef } from "react"
import styled from "styled-components"

const Container = styled.div``

const Posts = ({ users, setUsers, setOnEdit }) => {

    return (
        <Container>
            <h2>Postagens</h2>
            <hr />
        </Container>
    )
}

export default Posts