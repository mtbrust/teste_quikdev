import React, { useRef } from "react"
import styled from "styled-components"

const Container = styled.div``

const Post = ({ users, setUsers, setOnEdit }) => {

    return (
        <Container>
            <h2>Post</h2>
            <hr />
        </Container>
    )
}

export default Post